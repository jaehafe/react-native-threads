import { SafeAreaView, StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function TabScreenTwo() {
  return (
    <SafeAreaView>
      <Text>TabScreenTwo</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
