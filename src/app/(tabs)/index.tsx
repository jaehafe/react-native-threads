import { SafeAreaView, StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function TabScreenOne() {
  return (
    <SafeAreaView>
      <Text>TabScreenOne</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
